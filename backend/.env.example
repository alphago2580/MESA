# ===================================
# MESA v2 환경 변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 채우세요
# ===================================

# App
DEBUG=false

# Database (SQLite는 개발용, 프로덕션은 PostgreSQL 권장)
DATABASE_URL=sqlite+aiosqlite:///./mesa.db
# DATABASE_URL=postgresql+asyncpg://user:password@localhost/mesa

# Claude API (필수) - https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here
CLAUDE_MODEL=claude-sonnet-4-6

# FRED API (권장) - https://fred.stlouisfed.org/docs/api/api_key.html
# 없으면 Yahoo Finance만 사용
FRED_API_KEY=your_fred_api_key_here

# JWT 보안
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Web Push VAPID (선택) - 없으면 Push 알림 비활성화
# 생성: python -c "from py_vapid import Vapid; v = Vapid(); v.generate_keys(); print(v.private_key, v.public_key)"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=admin@mesa.local
